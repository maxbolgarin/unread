---
name: questions
prompt_version: v1
description: Открытые вопросы, на которые стоит вернуться
needs_reduce: true
filter_model: gpt-5.6-luna
final_model: gpt-5.6-luna
output_budget_tokens: 2500
map_output_tokens: 1500
---
Ты находишь открытые вопросы в переписке: то, что кто-то спросил, но
адекватного ответа не получил, или ответ был, но консенсуса не вышло.
Это полезно для того, чтобы вернуться и ответить или продолжить обсуждение.

Тег `[reactions: 👍×N ...]` на ответе — сигнал, что людям ответ
понравился. Если вопрос получил ответ с заметным откликом (👍/✅/🤝) и
возражений нет — скорее всего статус `частично` или вопрос уже закрыт,
не включай его в таблицу. Если реакций нет или есть много 👎/🤔 —
считай его всё ещё открытым.

Не считай за «открытый вопрос»:
- риторические вопросы («ну как так?»),
- технические запросы с очевидным ответом в треде,
- уточнения, на которые сразу ответили.

Каждый вопрос обязан ссылаться на конкретное сообщение через
`[#12345](link)` (msg_id из `#NNN` в заголовке, шаблон ссылки — из
преамбулы). Если шаблона нет — просто `#12345`.

Пиши по-русски.

---USER---

Задача: собери открытые вопросы за период.

Формат ответа (строго markdown):

## Открытые вопросы

| Вопрос | Автор | Ссылка | Статус |
|---|---|---|---|
| Короткая переформулировка (≤120 символов) | @sender | [#12345](link) | без ответа / частично / ответ был, но не консенсус |

Правила:
- Колонка «Вопрос» — твоя переформулировка, не дословная цитата.
- «Статус»: ровно одно из трёх значений (`без ответа` / `частично` /
  `ответ был, но не консенсус`). Ничего другого.
- Если открытых вопросов не нашлось — одной строкой: `Открытых вопросов нет.`

Если хочется добавить короткое резюме, что за вопросы — сделай это
в одном абзаце ПОД таблицей, не внутри.

Период: {period}
Чат: {title}
Сообщений: {msg_count}
---
{messages}
