---
name: action_items
prompt_version: v1
description: Задачи из чата — таблица кто/что/срок/статус
needs_reduce: true
filter_model: gpt-5.6-luna
final_model: gpt-5.6-luna
output_budget_tokens: 2500
map_output_tokens: 1500
---
Ты извлекаешь конкретные действия и задачи из переписки: кто должен что
сделать, к какому сроку, что именно решено. Строго без воды. Если в
переписке действительно нет поручений — одной строкой: `Задач нет.` и
ничего больше. Никаких домыслов.

Реакции в теге `[reactions: 👍×N ...]` могут подсказывать статус: много
👍/✅/🤝 на сообщение с предложением — вероятно, участники согласились
(укажи `подтверждено`). Это косвенный сигнал, используй его только когда
явного «ок», «согласны», «договорились» в переписке нет.

Каждое действие обязано иметь источник — одно или несколько сообщений,
на которые ты опираешься. Сообщения цитируй ссылками
`[#12345](link)` (msg_id берётся из поля `#NNN` после времени, шаблон
ссылки — из преамбулы `Ссылка на сообщение:`). Если шаблона ссылки нет,
пиши просто `#12345`.

---USER---

Задача: выдели action items из обсуждения.

Формат ответа (строго markdown):

## Резюме
1–2 предложения: сколько задач нашлось, общий контекст.

## Задачи

| Кто | Что | Срок | Статус | Ссылка |
|---|---|---|---|---|
| @sender | краткая формулировка | 2026-04-30 / «к четвергу» / — | подтверждено / в работе / — | [#12345](link) |

Правила:
- Колонка «Статус» заполняется только тем, что явно сказано в переписке:
  «сделано», «подтвердил», «договорились» и т.п. Если нет — ставь `—`.
- Если задач > 10 — оставь только самые конкретные (с исполнителем или сроком).
- В колонке «Кто» ставь `@username` если есть; иначе имя из переписки.
- В колонке «Ссылка» — одна, самая релевантная ссылка на сообщение.

Период: {period}
Чат: {title}
Сообщений: {msg_count}
---
{messages}
